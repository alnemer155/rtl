# FastAPI + SQLAlchemy + الزاحف — اعتماديات Python الموحدة
# يفضّل التثبيت من pyproject.toml: pip install -e ".[dev]"
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0.30
alembic>=1.13
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
tenacity>=8.3
aiolimiter>=1.1
pydantic>=2.7
pydantic-settings>=2.3

# للاختبارات وفحوصات الجودة
pytest>=8.2
ruff>=0.5
mypy>=1.10
types-beautifulsoup4

# لدعم PostgreSQL (اختياري)
# psycopg[binary]>=3.2
